//
//(a)vector<vector<int>> ivec;
//对ivec执行默认初始化，得到一个空的容纳vector<int>的vector
//(b)vector<string> svec=ivec;
//非法,ivec类型为vector<vector<int>>与svec类型vector<string>不匹配
//(c)vector<string> svec(10,"null");
//10个string类型的元素，每个都是null